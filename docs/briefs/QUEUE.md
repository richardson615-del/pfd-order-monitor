# Work queue — pfd-order-monitor

Claude Code: when Nick says **"Work the queue"**, take the top `open` item, set Status to `claimed (<date>)`, commit that one-line change on your branch, do the work as its own PR, set Status to `PR #n`, take the next. Stop at an item marked **STOP** — it needs Nick. Read the brief first; the brief wins if this file is stale. Only one Claude Code session works this repo at a time.

**Merging (Nick's decision 2026-09-16): PRs merge themselves.** After CI is green, run `gh pr merge --auto --squash <n>` on your own PR. If that command is denied or auto-merge is off for the repo, STOP and tell Nick — do not merge any other way. Stacked PRs: enable auto-merge on the base first; retarget the child to `main` after the base merges. Cross-repo features: this repo's PR merges before the CRM's.

| # | Item | Brief | Status |
|---|---|---|---|
| **0a** | **HOTFIX (2026-09-17, blocks every new tablet): `POST /api/crm/tablets/bind`, `POST /api/crm/tablets/link` and `POST /api/kiosk/bootstrap` look restaurants up by `restaurants.id` only. The CRM sends its `accounts.id`, which is stored in `restaurants.crm_restaurant_id` (migration 007; `lib/restaurant-resolve.ts` creates rows with a fresh uuid). FACT from prod roster 2026-09-17: Twisted Fork bridge id `371eaaa3-…` ↔ CRM `9ae1f031-…`; Willie Mae's `940bf644-…` ↔ `45bef1a1-…`. So the CRM's assign push and Link tablet both fail with "restaurant not found" for every restaurant. Fix on `fix/bind-resolve-crm-id`: one helper `resolveRestaurantRef(ref)` = `id` OR `crm_restaurant_id`, used by bind, link, bootstrap and `/api/crm/restaurants/:id` (GET/POST/provision) so every CRM-facing route accepts either id; unit test both paths; update `docs/crm-bridge-contract.md` ("`restaurant_id` in CRM calls is the CRM account id; the bridge resolves it") and correct `C:\Users\richa\dev\CLAUDE.md` §2 which wrongly states the ids are equal. Then confirm kiosk device `R8YL42958HD` (Twisted Fork, code 825967) binds. Auto-merge. | docs/briefs/2026-09-16-kiosk-first-run-and-orders.md I1 | PR #72 — auto-merge; confirm R8YL42958HD binds after deploy |
| **0b** | **I3 — Accept + 25-minute countdown (Nick, 2026-09-17, supersedes I2's "no Accept step"):** order chimes until **Accept** (card + ticket); Accept starts a 25:00 countdown (per-restaurant `prep_minutes`, default 25) that goes amber <5:00, red at 0 and counts up, never auto-completes; **Complete** replaces Done; Completed shows real prep minutes. Web-only. Take right after 0a. | docs/briefs/2026-09-17-accept-countdown.md | PR #73 — merged 2026-09-17, live (build c0d85aa5, migration 040 applied) |
| **0c** | **I4 — "ANOTHER ONE!" new-order alert (Nick, 2026-09-17):** replace the synthesized two-tone new-order chime with a voice clip `public/sounds/another-one.mp3` played through the existing armed AudioContext (prefetch+decode in `armAudio()`, buffer cached, chime kept as fallback). Repeats every 8 s until Accept as today. The I3 0:00 tone stays a plain tone (`playOvertimeTone()`). The clip is already committed (`public/sounds/another-one.mp3`, Matt's recording, v4 approved) — use it as-is, no TTS placeholder. Web-only. Take next. | docs/briefs/2026-09-17-another-one-alert.md | PR #74 |
| **0d** | **M1 — CRM orders API (Nick, 2026-09-18):** `GET /api/crm/orders?date&tz&restaurant_id&include_test&since` (local-day list, restaurant ref resolved either id, `phone_last4` only, `items_line`, destinations, print state, flags, sorted like the tablet) + `GET /api/crm/orders/:id` (full ticket: items, customer, notes, money, timeline, print jobs, app push; never raw_html/raw_payload) + `POST /api/crm/orders/:id/actions` {reprint|resend_app} **only if a re-enqueue/re-push primitive already exists** — otherwise skip and add M3 open. Document in the contract incl. the undocumented `/api/crm/accounting/orders`. Merges before the CRM's M2. Take next. | docs/briefs/2026-09-18-orders-dashboard.md | PR #76 — actions shipped (both primitives existed), so no M3 |
| **0f** | **REVERT (Nick, 2026-09-18): new-order alert back to the two-tone chime.** In `lib/sound.ts` make the default new-order sound `playChime()` again; keep the clip code path and `public/sounds/another-one.mp3` but behind `NEXT_PUBLIC_NEW_ORDER_ALERT=clip` (default `chime` when unset) so it can be turned back on without a rebuild. Prefetch of the clip only when the flag is `clip`. 8 s repeat, 6 h ceiling, `playOvertimeTone()` unchanged. Update the I4 brief header ("reverted 2026-09-18") and tests (chooser picks chime by default, clip only with the flag). Small PR, **take this first — it is in live kitchens.** | docs/briefs/2026-09-17-another-one-alert.md | PR #78 — chime by default; `NEXT_PUBLIC_NEW_ORDER_ALERT=clip` brings the voice back |
| **0e** | **M1b — Zuppler back-link (Nick, 2026-09-18; brief §M1.4, added after 0d started):** `GET /api/crm/orders/:id` gains `zuppler: {order_uuid, restaurant_id, admin_url}` for `source = "zuppler"`. Order of work: (a) inspect `raw_payload` of a recent Zuppler order in the dev DB for any admin/CS/order URL field — if present, store on ingest as nullable `orders.source_admin_url` (idempotent migration, `REQUIRED_SCHEMA`) and return it; (b) else build from env `ZUPPLER_ORDER_URL_TEMPLATE`. **FACT (Nick, 2026-09-18, real link from Zuppler customer service):** `https://customer-service.zuppler.com/#/lists/5cc7eab5-ea1d-4985-b864-92afc523eb1e/order/5a06d750-3cd8-4c09-913d-8000f676a445` → template default `https://customer-service.zuppler.com/#/lists/{list_id}/order/{order_uuid}` with `{list_id}` from env `ZUPPLER_CS_LIST_ID` (Nick's value above; ASSUMPTION it is one saved list for all restaurants — if a Zuppler order's `external_id` is not a uuid, or the dev DB shows the list id differs per restaurant, STOP and say so). Verify `orders.external_id` for `source=zuppler` is the same uuid shape as the order segment before wiring; `null` when env unset. **Never guess beyond that.** Tests for substitution + null; contract updated. Small PR, take next. | docs/briefs/2026-09-18-orders-dashboard.md §M1.4 | PR #77 (template) + PR #79 (the real CS shape as the default, `{list_id}` from `ZUPPLER_CS_LIST_ID`; external_id verified = order.uuid in the mapper; non-uuid external_id → null). **Nick: set `ZUPPLER_CS_LIST_ID=5cc7eab5-ea1d-4985-b864-92afc523eb1e` in Vercel (production)** — until then `admin_url` is null |
| 0 | Enable auto-merge on the open PRs in this order: #62, then #63 (retarget base to main after #62 merges), then #64 | — | done 2026-09-16 — #62, #63, #64 merged (and #65, #66). Repo auto-merge is now ON |
| 1 | E3 follow-up `feat/poll-first`: Realtime opt-in (env flag, default off), incremental poll "changed since last sync" | docs/scale-500.md §6 | PR #65 — merged 2026-09-16 |
| 2 | C4 leftovers: `MIN_SHELL_VERSION` gate + `/api/version` idle-reload verified on 1.3.0 — **STOP before setting MIN_SHELL_VERSION=5 in Vercel; Nick confirms fleet is on 1.3.0** | docs/briefs/2026-09-14-premium-brand-dashboard-alerts.md | done 2026-09-18 — Nick set `MIN_SHELL_VERSION=5`; production `/api/version` reports `minShellVersion: 5`, so a tablet on shell < 5 now shows the amber line and the CRM's shell chip turns amber |
| 3 | Kiosk acceptance script: `scripts/test-kiosk-flow.ts` covering bootstrap bound/unbound/throttled, link code, unseen(), buckets, per-day aggregation in America/Chicago | docs/briefs/2026-09-16-kiosk-first-run-and-orders.md | PR #67 |
| 4 | Ready screen "Send me a test order" wired to `/api/dashboard/test-order`; auto-advance 20 s | same | done — shipped in #61 (I1): `ReadyScreen.tsx` posts to `/api/dashboard/test-order`, `READY_AUTO_ADVANCE_MS = 20_000`; pinned by `scripts/test-first-run.ts` and `test-kiosk-flow.ts` |
| 5 | D3b test hygiene (whatever is still open) | docs/briefs/2026-09-15-tablet-fleet-and-followups.md | done — nothing open: prs-crm #50 (Node 24 in engines + CI), #56 (two timeouts + the stranding teardown), #72 (integration suite in CI on postgres:17, TEST_DB_ALLOWLIST), #117 (CI builds the app); latest merged PR #160 green |
| 6 | Pre-launch load test against a dev deployment — **STOP: needs Nick to create the dev deployment + a tablet login** | docs/scale-500.md | open — STOP (Nick) |
| 7 | Rotate Hexnode API key + update Vercel — **STOP: Nick does this in Hexnode Admin → API before first fleet ship** | — | open — STOP (Nick) |
| 8 | PR #48 (2026-09-15): Gmail poll shared-secret check fails open when `CRON_SECRET` is unset and compares with `!==` — rebase on main, CI, merge | PR #48 body | PR #48 — merged 2026-09-16 (rebased over E3's timer line); production c6bb846 answers 401 to no header and to `Bearer undefined`, so `CRON_SECRET` is set there |
| **9** | **O1 — Phone-order ingest for the CRM (Nick, 2026-09-18, Workstream O; merge before CRM O4):** `POST /api/crm/orders` (Bearer `CRM_WRITE_KEY`, zod, `source:'phone'`, idempotent on `(source, external_id)` → 200 on retry, 409 on same key/different payload, 422 unknown restaurant; accepts CRM account id or bridge id) inserted through the same canonical helper Zuppler ingest uses so `orderDestinations()` / print jobs / tablet push / `orders.status` are untouched; ticket shows **PAID — CARD ••1234** or **CASH DUE $x.xx**, never "Zuppler"; admit `'phone'` in any `source` constraint (migration). Plus `GET /api/crm/restaurants/:id/menu` **only if** the existing Zuppler GraphQL client can load a menu — otherwise record "no menu source" in the contract. Update `docs/crm-bridge-contract.md`. First read `lib/canonical.ts` + the Zuppler client and cite them in the PR. | docs/briefs/2026-09-18-manual-order-entry.md §3 O1 | PR #84 — auto-merge on. Menu route skipped (LoadOrder is the only Zuppler query — "menu source: none" in the contract; O3 starts from channel-json). Migration 042 (041 is PR #83's). Acceptance needs Nick: curl the contract sample at a dev restaurant → prints/chimes, second POST → 200 not a second ticket |
| **10** | **R1 — Relink a bridge restaurant to another CRM account (Nick, 2026-09-24; unblocks prs-crm row 60 account merges):** `POST /api/crm/restaurants/:id/relink { crm_restaurant_id, actor }` (CRM_WRITE_KEY, either id) changes only `restaurants.crm_restaurant_id`; idempotent (`changed:false`), 409 `crm_account_taken` naming the other restaurant, 400 non-uuid, 404 unknown; audit row; refresh anything cached by crm id; accounting orders by the new id include pre-relink orders (STOP and report if any table stores crm id per order); document in crm-bridge-contract.md. Tests per brief. Merges before prs-crm row 60 step 1. | docs/briefs/2026-09-24-relink-restaurant-crm-account.md | PR #85 — merged 2026-09-24, production 60dfcd7, migration 043 applied (/api/health schema ok); prs-crm #346 (row 60 step 1) calls it. Route + migration 043 (`restaurant_link_audit`) + contract + `scripts/test-relink.ts`. No table stores a CRM id per order and nothing is cached by it (every side table keys on `restaurants.id`). Migration 043 applies on deploy. Then prs-crm row 60 step 1 can call it |

Done today (2026-09-16): #60 I2, #61 I1, #62 alert copy, #63 D1, #64 stale-print, #65 poll-first, #66 queue file — all merged; production build 515565f, migrations 037–039 applied. This file replaces docs/work-queue.md. APK 1.3.0 (code 5) uploaded to Hexnode; policy Premium #1 v6 has device_ref=%serialnumber% and notifications=Allow.
